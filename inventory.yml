---
k3s_cluster:
  children:
    server:
      hosts:
        192.168.31.98:
    agent:
      hosts:
        192.168.31.99:

  # Required Vars
  vars:
    ansible_port: 22
    ansible_user: pdk
    k3s_version: v1.36.1+k3s1
    enable_api_ha: false
    #vip: 192.168.31.80
    #vip_master_interface: enp0s3
    #vip_backup_interface: enp0s3
    #vip_router_id: 52
    #haproxy_port: 443
    # The token should be a random string of reasonable length. You can generate
    # one with the following commands:
    # - openssl rand -base64 64
    # - pwgen -s 64 1
    # You can use ansible-vault to encrypt this value / keep it secret.
    # Or you can omit it if not using Vagrant and let the first server automatically generate one.
    # token: "pt8SXBHof4sCeJffGQK4tfwcfT6t541829et5VB/eOkbhbxvtMLhtWmX5h/8hBI5CzGdoQMfmZthBt6kWf2M/g=="
    token: "L9PKl6UsXZCIfMm8LHv8ihu1AfoCLGa3Mb1v5f6EBjwdON1T0p7RQBdL+aB09yxqdQtrTfBZ8mixDVPo3fyU2g=="
    api_endpoint: "{{ hostvars[groups['server'][0]]['ansible_host'] | default(groups['server'][0]) }}"

    # Optional vars
    extra_server_args: "--flannel-backend=none --disable-network-policy --cluster-cidr=10.10.0.0/16"
    # extra_agent_args: ""
    # cluster_context: k3s-ansible
    # api_port: 6443
    # k3s_server_location: /var/lib/rancher/k3s
    # systemd_dir: /etc/systemd/system
    # extra_service_envs: [ 'ENV_VAR1=VALUE1', 'ENV_VAR2=VALUE2' ]
    # user_kubectl: true, by default kubectl is symlinked and configured for use by ansible_user. Set to false to only kubectl via root user.

    # Manifests or Airgap should be either full paths or relative to the playbook directory.
    # List of locally available manifests to apply to the cluster, useful for PVCs or Traefik modifications.
    # extra_manifests: [ '/path/to/manifest1.yaml', '/path/to/manifest2.yaml' ]
    # airgap_dir: /tmp/k3s-airgap-images

    # server_config_yaml:  |
    #   This is now an inner yaml file. Maintain the indentation.
    #   YAML here will be placed as the content of /etc/rancher/k3s/config.yaml
    #   See https://docs.k3s.io/installation/configuration#configuration-file
    # agent_config_yaml:  |
    #   Same as server_config_yaml, but for the agent nodes.
    #   YAML here will be placed as the content of /etc/rancher/k3s/config.yaml
    #   See https://docs.k3s.io/installation/configuration#configuration-file
    registries_config_yaml:  |
      mirrors:
        docker.io:
          endpoint:
            - "https://harbor.pengdake.top/v2/proxy-cache"
        registry.k8s.io:
          endpoint:
            - "https://k8s.m.daocloud.io"
        quay.io:
          endpoint:
            - "https://harbor.pengdake.top/v2/quay-proxy-cache"
        harbor.pengdake.top:
          endpoint:
            - "https://harbor.pengdake.top"
        "*":
          endpoint:
            - "http://192.168.31.92:5000"
      configs:
        "harbor.pengdake.top":
          tls:
            ca_file: /etc/rancher/k3s/harbor-ca.crt
            cert_file: /etc/rancher/k3s/harbor-tls.crt
            key_file: /etc/rancher/k3s/harbor-tls.key
        "*":
          tls:
            insecure_skip_verify: true
    #   Containerd can be configured to connect to private registries and use them to pull images as needed by the kubelet.
    #   YAML here will be placed as the content of /etc/rancher/k3s/registries.yaml
    #   See https://docs.k3s.io/installation/private-registry
